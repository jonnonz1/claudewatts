#!/usr/bin/env bash
# Thin wrapper so `cwatts` works as a command name.
# Forwards every argument to claudewatts.py.
exec python3 "$(dirname "$(readlink -f "$0" 2>/dev/null || echo "$0")")/claudewatts.py" "$@"
